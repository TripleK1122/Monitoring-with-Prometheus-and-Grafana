# Project 4 — Monitoring with Prometheus & Grafana

## Run the stack
```bash
docker-compose up -d
Access

Prometheus → http://localhost:9090

Grafana → http://localhost:3000

Username:

Password: 

Stop
docker-compose down


---

✅ 5. Check the Launch
Now, run the stack:

```bash
docker-compose up -d